#include <stdio.h>
#include "answer.h"

int answer(int questionNo)
{
    if (questionNo==1)
        return 15;
    else if (questionNo==2)
        return 10;
    else if (questionNo==3)
        return 2;
    else if(questionNo==4)
        return 4;
}